{
  "name": "js-protovalidate-example",
  "version": "1.0.0",
  "description": "Example of using protovalidate-es with bufrnix",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "validate": "node dist/validate.js",
    "test": "npm run build && npm run validate"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^1.0.0",
    "@bufbuild/protovalidate": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
