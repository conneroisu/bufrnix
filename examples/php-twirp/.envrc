# Use nix flakes for development environment
use flake

# Print a helpful message when entering the directory
cat << EOF
🚀 Bufrnix PHP Twirp Example

This example demonstrates using the PHP language module in bufrnix 
with the protoc-gen-twirp_php plugin.

To get started:
  1. Run 'setup-twirp-php' to generate code with bufrnix and install dependencies
  2. Run 'run-server' in one terminal to start the server
  3. Run 'run-client' in another terminal to test the client

You can also run 'bufrnix' directly to only generate the code.

See README.md for more information
EOF